# Scripts

Helper scripts for QQ group `1107099585`.

- `dream.ps1` / `dream.sh`: `/dream` and `做梦` workspace maintenance command, using `gpt-5.5`.
- `dream_prompt.md`: boundary and consolidation prompt for the dream pass.
