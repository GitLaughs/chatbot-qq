You are running `/dream` for the QQ group workspace `C:\chatbot-qq\groups\sandbox-123456789`.

Purpose: run a deliberate memory-maintenance pass for this QQ group: orient, gather, consolidate, prune, and report.

Secondary purpose: review whether the deterministic memory, file, rule, and test
code can be improved without increasing server load or widening runtime powers.
Use `../../../docs/dream-review-template.md` as the shared self-iteration policy
when it is available.

Hard boundaries:

- Only read, write, list, or summarize files under this workspace.
- Do not follow junctions, symlinks, or reparse points.
- Do not read private chats, credentials, browser data, global memory, cloud files, or files outside this workspace.
- Do not call QQ, Feishu, email, calendar, contact, task, or private-message APIs.
- Network is allowed only for public project context directly referenced by files in this workspace.
- Do not edit `AGENTS.md` or `INSTRUCTIONS.md` unless a contradiction makes the dream impossible; if so, report instead of editing.
- Do not auto-edit production code.
- Do not apply patches generated by this dream pass.
- Do not touch secrets, env files, tokens, cookies, provider keys, or browser data.
- Do not enable cross-group search by default.
- Do not restart, reload, deploy, or trigger runtime hot updates.
- Do not add always-on vector databases, local LLM daemons, embedding rebuild loops, per-message LLM summaries, recursive review loops, or other heavy background jobs.
- Keep the architecture fixed as NapCat/OneBot + onebot-group-proxy + cc-connect + local groups/users workspaces; do not switch this workspace toward official QQ Bot APIs.

Workflow:

1. Orient
   - Read `AGENTS.md`, `INSTRUCTIONS.md`, `KNOWLEDGE.md`, `local_files/INDEX.md`, and recent `memory/*.md` files if present.
   - Read the compact evidence packet listed in Run context as the chat evidence for this pass.
   - Do not read raw `memory/chat-YYYY-MM-DD.jsonl` or `memory/file-events-YYYY-MM-DD.jsonl` by default.
   - Build a short map of what the workspace currently knows.

2. Gather
   - Inspect the local file tree under `local_files/` by names, paths, sizes, and obvious text summaries.
   - Inspect `members/` only for stable, message-backed observations.
   - For large files, do not deeply parse content unless an existing index points to a missing summary.
   - Ignore source-map files unless the user explicitly asks for forensic debugging.
   - Check whether `INDEX.md`, `KNOWLEDGE.md`, member notes, and `memory/YYYY-MM-DD.md` disagree.

3. Consolidate
   - Add durable facts, decisions, and useful group state into `KNOWLEDGE.md`.
   - Add raw notes or a dream log into `memory/YYYY-MM-DD.md`.
   - If chat evidence shows the bot's local voice should adapt, update `memory/bot-style.md` with short notes about tone, humor, callbacks, reply length, and when to stay quiet.
   - Bot style may evolve like a long-term group participant, but do not invent a fake human identity or offline experiences.
   - If helpful, create a detailed report under `memory/dreams/YYYY-MM-DD-HHMMSS.md`.
   - Keep updates concise. Do not duplicate facts already stated well.
   - For bot implementation ideas, write only review notes, deterministic rule suggestions, candidate patches, and test suggestions. Do not edit production source files from this pass.

4. Prune
   - Mark stale or superseded notes only when there is clear evidence.
   - Do not delete original files.
   - Do not remove user-authored project material.

5. Report
   - Final reply must be short and suitable for QQ group chat.
   - Include changed file paths and the 3-6 most useful findings/actions.
   - If no useful changes were needed, say so and explain what was checked.
   - If self-iteration ideas were found, include: review conclusion, boundary risks, deterministic rule suggestions, candidate patches not applied, test suggestions, and not executed.
