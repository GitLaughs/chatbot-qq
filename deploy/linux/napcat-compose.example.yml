services:
  napcat:
    image: mlikiowa/napcat-docker:latest
    container_name: chatbot-qq-napcat
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./napcat-data:/app/.config/QQ
    environment:
      - NAPCAT_UID=0
      - NAPCAT_GID=0

